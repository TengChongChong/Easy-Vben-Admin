import type { App } from 'vue';

import {
  Alert,
  Avatar,
  Button,
  Badge,
  Checkbox,
  Descriptions,
  Empty,
  Layout,
  List,
  Card,
  Cascader,
  Divider,
  Dropdown,
  Menu,
  Table,
  Tabs,
  Tag,
  Tree,
  Timeline,
  Form,
  Tooltip,
  Typography,
  Popconfirm,
  Popover,
  Input,
  InputNumber,
  Upload,
  Radio,
  Row,
  Result,
  Select,
  Skeleton,
  Spin,
  Col,
} from 'ant-design-vue';

export function registerGlobComp(app: App) {
  app
    .use(Alert)
    .use(Avatar)
    .use(Button)
    .use(Badge)
    .use(Descriptions)
    .use(Empty)
    .use(Layout)
    .use(List)
    .use(Card)
    .use(Cascader)
    .use(Checkbox)
    .use(Divider)
    .use(Dropdown)
    .use(Menu)
    .use(Table)
    .use(Tabs)
    .use(Tag)
    .use(Tree)
    .use(Timeline)
    .use(Tooltip)
    .use(Form)
    .use(Typography)
    .use(Popconfirm)
    .use(Popover)
    .use(Input)
    .use(InputNumber)
    .use(Upload)
    .use(Radio)
    .use(Row)
    .use(Result)
    .use(Select)
    .use(Skeleton)
    .use(Spin)
    .use(Col);
}
